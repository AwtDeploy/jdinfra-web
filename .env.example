# Copy to .env for local dev. On Vercel, add the same keys in Project → Settings → Environment Variables.
# See VERCEL_DEPLOY.md — never commit .env to Git.

# MongoDB Atlas
MONGODB_URI=mongodb+srv://gayathriparsam06_db_user:<db_password>@jdinfra.tn1cfht.mongodb.net/?retryWrites=true&w=majority&appName=JDInfra
MONGODB_DB_NAME=jdinfra
MONGODB_COLLECTION=enquiries

# Resend (email API - works on Vercel/Netlify Functions)
RESEND_API_KEY=re_xxxxxxxxx
SMTP_FROM="JD Infra Website <onboarding@resend.dev>"

# Where enquiry emails are delivered
CLIENT_EMAIL=jdinfradeveloper@gmail.com